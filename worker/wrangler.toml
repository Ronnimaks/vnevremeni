name = "vnevrem-booking"
main = "src/index.js"
compatibility_date = "2025-09-01"

# Домены, с которых разрешено принимать заявки. Через запятую, без слеша на конце.
# После покупки домена сюда добавляется он же — иначе форма получит 403.
[vars]
ALLOWED_ORIGINS = "https://vnevremeni-club.ru,https://www.vnevremeni-club.ru,https://ronnimaks.github.io,https://vnevremeni.pages.dev"

# Секреты задаются отдельно и в файл не пишутся:
#   npx wrangler secret put TELEGRAM_BOT_TOKEN
#   npx wrangler secret put TELEGRAM_CHAT_ID
