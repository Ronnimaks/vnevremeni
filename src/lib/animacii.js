// Набор возможностей библиотеки анимаций, который нам действительно нужен.
//
// По умолчанию она приезжает целиком: перетаскивание мышью, плавные перестроения
// вёрстки, пружины, значения по прокрутке. Ничего из этого на сайте нет —
// проверено по коду. Мы используем только появление, исчезновение и показ
// блоков при прокрутке, а это как раз domAnimation.
//
// Файл отдельный, потому что подгружается он не сразу: сначала на телефон едет
// то, что человек видит, а возможности анимаций доезжают следом.
import { domAnimation } from 'framer-motion';

export default domAnimation;
